{
  "name": "mindmap",
  "version": "0.1.0",
  "private": true,
  "description": "Keyboard-first mind mapping app (static, no build step)",
  "scripts": {
    "start": "python3 -m http.server 8080"
  }
}
